# Discrepancy Log

Use this file to record operational mismatches, ambiguities, or conflicts that matter to bounded execution.

Suggested fields:
- date
- task family
- discrepancy
- impact
- current status
- next action

## Current status

| date | task family | discrepancy | impact | current status | next action |
|---|---|---|---|---|---|
| 2026-04-13 | `always_full_init_semantics_pass` | Earlier bootstrap semantics inferred `workflow_profile` values like `lite`, `standard`, and `fullstack`, which risked making init look responsible for project-architecture decisions it cannot safely make. | Init semantics were misleading and could encourage premature architecture assumptions instead of letting supervision recover repo state and check with the user first. | Resolved. [E] | Keep successful init limited to installing the same full SUPLEX control layer, rewriting truthful target-state docs, writing `.suplex/init_state.yaml`, printing the supervision bootstrap prompt, and stopping. [E] |
| 2026-04-13 | `packaging_layout_refactor_pass` | The repository root previously exposed the SUPLEX kernel directly, which made the project repo look like an initialized target repo instead of a distributable packaging repo. | Packaging boundaries were unclear and bootstrap source assumptions depended on a root-level payload. | Resolved. [E] | Keep the copied target-repo payload under `template/` and source bootstrap copies from there. [E] |
| 2026-04-13 | `kernel_scaffold_packaging_pass` | The template repo now includes its own packaging `README.md`, which could be confused with the target repo's required seed `README.md` unless the distinction is made explicit. | Misreading the packaging README as initialization input could cause an incorrect future initialization pass. | Resolved. [E] | Keep the distinction explicit in template packaging docs and initialization behavior. [E] |
| 2026-04-13 | `controlled_target_repo_validation_pass` | Applying the current template by straight control-layer copy leaves repo-state docs such as `docs/00_project_scope.md` and `docs/09_supervision_brief.md` describing `suplex-template` instead of the target repo. | Supervision with repo access becomes inconsistent and the portable supervision packet is not trustworthy without extra reconstruction, which blocks safe first real-world use. | Resolved by revalidated init behavior. [E] | During target-repo init, rewrite `docs/00_project_scope.md`, `docs/08_status_checkpoint.md`, and `docs/09_supervision_brief.md` before supervision bootstrap or portable-packet use. [E] |
| 2026-04-13 | `bootstrap_layer_pass` | The thin bootstrap wrappers now support remote archive fetch, but this bounded pass validated only the local source-root path in controlled targets because live network fetch was outside the local validation setup. | Remote distribution behavior still needs one real-world bounded check before declaring the packaging path fully validated. | Open. [E] | Run the documented remote bootstrap command inside a non-temporary target repo and record any packaging or fetch-path defects. [E] |
| 2026-04-13 | `bootstrap_interpreter_detection_repair_pass` | Real-world wrapper use exposed that interpreter discovery was brittle on common environments where POSIX shells expose `python3` but not `python`, or Windows exposes `py -3` but not a usable `python`. | Remote bootstrap could fail before SUPLEX init even when Python 3 was actually available on the host. | Resolved by wrapper probe repair. [E] | Keep wrapper interpreter selection limited to executable Python 3 probes: POSIX `python3` then `python`; PowerShell `py -3` then `python`. [E] |
| 2026-04-14 | `supervisor_material_ambiguity_decision_rule_pass` | Earlier supervision docs required surfacing blockers, but they did not require the supervisor to explicitly ask whether the user wanted to resolve a material ambiguity directly or authorize best judgment. | User-owned judgment calls could be absorbed into supervision or delegated execution without an explicit decision path, reducing transparency and control. | Resolved. [E] | Require the supervisor to restate material blockers or ambiguities, ask whether the user wants to resolve them directly or authorize best judgment, and report any adopted assumption back in the pass record. [E] |
| 2026-04-14 | `handoff_template_material_ambiguity_record_pass` | The supervision rule for material ambiguities had no dedicated field in the handoff template, which left the recording pattern implicit and easy to skip. | The decision path and adopted assumption could be remembered in chat but omitted from the bounded contract or later report trail. | Resolved. [E] | Add a dedicated handoff-template section for the user decision path on material ambiguities and align execution/report expectations to restate any authorized assumption. [E] |
| 2026-04-14 | `workflow_mode_split_pass` | Earlier SUPLEX packaging exposed only one runtime infrastructure shape, which made lighter bounded work pay for dated history and extra reporting scaffolding even when the repo owner wanted a slimmer setup. | The control layer could feel heavier than necessary for short or low-friction bounded work, and the root placement of lesson / governance-memory files was structurally inconsistent with `docs/` as canonical control memory. | Resolved. [E] | Add explicit `standard` and `sans-sucre` workflow modes at initialization, keep separate active handoff and active execution-report artifacts in `sans-sucre`, and move lesson / governance-memory artifacts into `docs/`. [E] |
